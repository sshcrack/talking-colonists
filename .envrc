# .envrc — direnv auto-activation for docs
# Installs Python deps via uv when you cd into the project

uv venv --prompt talking-colonists-docs
source .venv/bin/activate
uv pip install --quiet -r docs/requirements.txt
