---
title: Commands
ai_instructions:
  goal: |
    Document all debug and utility commands provided by the mod.

  content_sections:
    - "**Command list**:"
      "  - `/mc_talking_debug` — Main debug command."
      "  - `/mc_talking_debug memory` — Debug memory state."
      "  - `/mc_talking_debug prompt` — Debug prompt state."
      "  - `/mc_talking_debug connections` — Debug active connections."
      "  - `/mc_talking_debug urgent_contact` — Debug urgent contact."
      "  - `/mc_talking_debug events` — Debug colony events."
      "  - `/mc_talking_debug status` — Debug mod status."
      "  - `/mc_talking_debug citizen` — Debug citizen state."
      "  - `/list_tools` — List all available AI tools."
    - "For each command, document:"
      "  - Syntax and arguments."
      "  - What information it displays."
      "  - Permission level (op required?)."
    - "**Source**: All command classes in the commands package."

  source_references:
    - "src/main/java/me/sshcrack/mc_talking/commands/"
---

# Commands

Debug and utility commands.

> **Note**: This is a stub page. Content should be populated per the `ai_instructions` above.
